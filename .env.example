# AI Draw Nexus 环境变量配置示例
# 复制此文件为 .env 用于本地开发

# 基础路径（生产环境部署在子路径时使用）
VITE_BASE_PATH=/

# AI API 基础 URL
VITE_API_BASE_URL=/dev-api/ai

# Toolbox API URL（用于数据流图节点关联功能）
# 本地开发：留空，使用 Vite 代理
# 生产环境：填写完整域名，如 http://mufengmufeng.cn
VITE_TOOLBOX_API_URL=

# Toolbox API 路径前缀
# 本地开发：/api（通过 Vite 代理转发到后端）
# 生产环境：/dev-api/api（因为 Nginx 会去掉 /dev-api/ 前缀）
VITE_TOOLBOX_API_PREFIX=/api

# 每日配额（未设置访问密码时的每日使用次数限制）
VITE_DAILY_QUOTA=10

# 是否使用流式响应
VITE_USE_STREAMING=true
